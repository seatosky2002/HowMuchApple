# 크롤링 통합 문서 (당근 · 번개장터 · 중고나라)

> 2026-07-17 기준. 서울·경기 애플 중고 매물 전량 수집을 위해 플랫폼 3사의 검색/지역 지정 동작을
> 조사·실험하고, 각각에 맞는 수집 방식을 설계·실행한 기록의 통합본.
> 원본: `daangn_seoul_gyeonggi_region_codes.md`, `bunjang_api_behavior.md`, `joongna_crawling_method.md`

## 플랫폼별 방식 한눈에 보기

세 플랫폼은 지역 검색 지원 방식이 전부 달라서, 수집 전략도 각각 다르다:

| | 당근 | 번개장터 | 중고나라 |
|---|---|---|---|
| 지역 지정 검색 | ✅ `?in=<동이름>-<regionId>` 필수 (지정해야만 해당 지역 매물이 나옴) | ❌ 필터 불가. 단 `order=distance`로 거리순 정렬 가능 | ❌ 웹에서 불가 (`locationFilter`는 앱 전용) |
| 채택한 수집 전략 | **동 코드 185개 순회** (서울 125 / 경기 60) | **단일 중심점(서울시청) 거리순 정렬** + 서울/경기 매물 끊기면 중단 | **전국 전량 수집** 후 `locationNames` 라벨로 서울/경기 필터 |
| 지역 정보 출처 | 검색 URL 자체가 지역 지정 | 매물의 `location` 필드 (~25%만 보유) | 검색 응답의 `locationNames` (택배 전용은 빈 배열) |
| 수집 결과 (2026-07-17) | — (동 코드 목록 문서) | 서울·경기 **7,366건** | 전국 6,288건 중 서울·경기 **2,700건** |
| 수집 스크립트 | `backend/scripts/crawl_daangn_all_dongs.py` | (서버에서 앱 크롤 타겟 재사용 실행) | `backend/scripts/crawl_joongna_all.py` |

---

# 1. 당근 — 서울·경기 동 코드 순회

당근 웹 검색은 지역지정 파라미터(`?in=<동이름>-<regionId>`)로 동네를 지정해야만
그 지역 매물이 나오는 구조다. 따라서 서울·경기 동 코드를 확보해 순회하는 방식을 쓴다.

**동 코드 수집 방법**: 알려진 앵커에서 BFS로 이웃 동네 링크를 따라가며 자동 수집했고,
각 지역 페이지의 `<title>`("시도 시군구 동 중고거래")로 소속을 검증해 서울특별시/경기도만 추렸다.

- **총 185개** (서울특별시 125 / 경기도 60), 수집 일자: 2026-07-17
- 동별 크롤 스크립트(`crawl_daangn_all_dongs.py`)는 원본 문서
  `docs/daangn_seoul_gyeonggi_region_codes.md`를 읽어 동작하므로 **원본 파일을 삭제하면 안 된다.**

## 서울특별시

### 강남구 (9개)

| 동 | 코드 |
|---|---|
| 논현동 | `논현동-6031` |
| 대치동 | `대치동-6032` |
| 도곡동 | `도곡동-6033` |
| 삼성동 | `삼성동-6034` |
| 신사동 | `신사동-382` |
| 압구정동 | `압구정동-385` |
| 역삼1동 | `역삼1동-392` |
| 역삼동 | `역삼동-6035` |
| 청담동 | `청담동-386` |

### 강동구 (7개)

| 동 | 코드 |
|---|---|
| 강일동 | `강일동-433` |
| 길동 | `길동-448` |
| 둔촌동 | `둔촌동-6040` |
| 상일동 | `상일동-434` |
| 성내동 | `성내동-6042` |
| 암사동 | `암사동-6043` |
| 천호동 | `천호동-6044` |

### 강북구 (6개)

| 동 | 코드 |
|---|---|
| 미아동 | `미아동-142` |
| 삼각산동 | `삼각산동-145` |
| 삼양동 | `삼양동-141` |
| 수유2동 | `수유2동-150` |
| 수유3동 | `수유3동-151` |
| 수유동 | `수유동-6046` |

### 강서구 (6개)

| 동 | 코드 |
|---|---|
| 내발산동 | `내발산동-6050` |
| 등촌동 | `등촌동-6051` |
| 마곡동 | `마곡동-6052` |
| 염창동 | `염창동-258` |
| 화곡동 | `화곡동-6057` |
| 화곡제1동 | `화곡제1동-262` |

### 관악구 (6개)

| 동 | 코드 |
|---|---|
| 낙성대동 | `낙성대동-345` |
| 난곡동 | `난곡동-361` |
| 대학동 | `대학동-358` |
| 봉천동 | `봉천동-6058` |
| 신림동 | `신림동-355` |
| 행운동 | `행운동-344` |

### 광진구 (6개)

| 동 | 코드 |
|---|---|
| 광장동 | `광장동-79` |
| 구의동 | `구의동-6059` |
| 군자동 | `군자동-73` |
| 자양동 | `자양동-6060` |
| 중곡동 | `중곡동-6061` |
| 화양동 | `화양동-72` |

### 구로구 (6개)

| 동 | 코드 |
|---|---|
| 개봉동 | `개봉동-6062` |
| 고척동 | `고척동-6063` |
| 구로동 | `구로동-6064` |
| 구로제1동 | `구로제1동-280` |
| 신도림동 | `신도림동-279` |
| 오류동 | `오류동-6066` |

### 금천구 (6개)

| 동 | 코드 |
|---|---|
| 가산동 | `가산동-295` |
| 독산동 | `독산동-6070` |
| 독산제1동 | `독산제1동-296` |
| 독산제3동 | `독산제3동-298` |
| 시흥동 | `시흥동-6071` |
| 시흥제1동 | `시흥제1동-300` |

### 도봉구 (6개)

| 동 | 코드 |
|---|---|
| 도봉동 | `도봉동-6077` |
| 방학동 | `방학동-6078` |
| 방학제1동 | `방학제1동-166` |
| 쌍문동 | `쌍문동-6079` |
| 창동 | `창동-6080` |
| 창제2동 | `창제2동-156` |

### 동대문구 (6개)

| 동 | 코드 |
|---|---|
| 답십리동 | `답십리동-6081` |
| 용두동 | `용두동-6083` |
| 이문동 | `이문동-6084` |
| 장안동 | `장안동-6085` |
| 전농동 | `전농동-6086` |
| 휘경동 | `휘경동-6087` |

### 동작구 (6개)

| 동 | 코드 |
|---|---|
| 노량진동 | `노량진동-6088` |
| 대방동 | `대방동-337` |
| 사당동 | `사당동-6091` |
| 상도동 | `상도동-6093` |
| 신대방동 | `신대방동-6094` |
| 흑석동 | `흑석동-331` |

### 마포구 (6개)

| 동 | 코드 |
|---|---|
| 공덕동 | `공덕동-223` |
| 상암동 | `상암동-237` |
| 서교동 | `서교동-230` |
| 성산동 | `성산동-6102` |
| 아현동 | `아현동-222` |
| 합정동 | `합정동-231` |

### 서대문구 (6개)

| 동 | 코드 |
|---|---|
| 남가좌동 | `남가좌동-6110` |
| 북가좌동 | `북가좌동-6116` |
| 연희동 | `연희동-211` |
| 창천동 | `창천동-6119` |
| 홍은동 | `홍은동-6124` |
| 홍제동 | `홍제동-6125` |

### 서초구 (7개)

| 동 | 코드 |
|---|---|
| 반포동 | `반포동-6126` |
| 방배동 | `방배동-6127` |
| 서초3동 | `서초3동-365` |
| 서초4동 | `서초4동-366` |
| 서초동 | `서초동-6128` |
| 양재동 | `양재동-6130` |
| 잠원동 | `잠원동-367` |

### 송파구 (6개)

| 동 | 코드 |
|---|---|
| 문정동 | `문정동-6184` |
| 석촌동 | `석촌동-417` |
| 오금동 | `오금동-414` |
| 위례동 | `위례동-425` |
| 잠실동 | `잠실동-6188` |
| 잠실본동 | `잠실본동-426` |

### 양천구 (6개)

| 동 | 코드 |
|---|---|
| 목1동 | `목1동-239` |
| 목5동 | `목5동-243` |
| 목동 | `목동-6190` |
| 신정3동 | `신정3동-253` |
| 신정4동 | `신정4동-254` |
| 신정동 | `신정동-6192` |

### 영등포구 (6개)

| 동 | 코드 |
|---|---|
| 대림동 | `대림동-6200` |
| 도림동 | `도림동-311` |
| 신길동 | `신길동-6207` |
| 여의도동 | `여의도동-6216` |
| 여의동 | `여의동-308` |
| 영등포본동 | `영등포본동-306` |

### 용산구 (6개)

| 동 | 코드 |
|---|---|
| 이촌동 | `이촌동-6244` |
| 이촌제1동 | `이촌제1동-46` |
| 이태원동 | `이태원동-6245` |
| 한강로동 | `한강로동-45` |
| 한남동 | `한남동-50` |
| 후암동 | `후암동-37` |

### 은평구 (6개)

| 동 | 코드 |
|---|---|
| 녹번동 | `녹번동-190` |
| 대조동 | `대조동-196` |
| 불광동 | `불광동-6255` |
| 역촌동 | `역촌동-200` |
| 응암동 | `응암동-6257` |
| 진관동 | `진관동-205` |

### 중랑구 (6개)

| 동 | 코드 |
|---|---|
| 망우동 | `망우동-6406` |
| 면목동 | `면목동-6407` |
| 묵동 | `묵동-6408` |
| 상봉동 | `상봉동-6409` |
| 신내동 | `신내동-6410` |
| 중화동 | `중화동-6411` |

## 경기도

### 고양시 일산동구 (1개)

| 동 | 코드 |
|---|---|
| 식사동 | `식사동-1550` |

### 고양시 일산서구 (2개)

| 동 | 코드 |
|---|---|
| 대화동 | `대화동-1568` |
| 탄현동 | `탄현동-1565` |

### 김포시 (4개)

| 동 | 코드 |
|---|---|
| 고촌읍 | `고촌읍-1753` |
| 구래동 | `구래동-1763` |
| 장기동 | `장기동-1762` |
| 풍무동 | `풍무동-1761` |

### 남양주시 (3개)

| 동 | 코드 |
|---|---|
| 다산동 | `다산동-4470` |
| 별내동 | `별내동-1604` |
| 호평동 | `호평동-1598` |

### 부천시 원미구 (6개)

| 동 | 코드 |
|---|---|
| 도당동 | `도당동-1418` |
| 상3동 | `상3동-1428` |
| 상동 | `상동-1425` |
| 심곡동 | `심곡동-4492` |
| 중1동 | `중1동-1421` |
| 중동 | `중동-1420` |

### 성남시 분당구 (6개)

| 동 | 코드 |
|---|---|
| 구미동 | `구미동-1355` |
| 백현동 | `백현동-1352` |
| 삼평동 | `삼평동-1351` |
| 서현동 | `서현동-4502` |
| 정자동 | `정자동-1339` |
| 판교동 | `판교동-1350` |

### 수원시 권선구 (6개)

| 동 | 코드 |
|---|---|
| 곡반정동 | `곡반정동-4525` |
| 구운동 | `구운동-1276` |
| 권선1동 | `권선1동-1278` |
| 권선동 | `권선동-4526` |
| 금곡동 | `금곡동-3964` |
| 호매실동 | `호매실동-3963` |

### 수원시 영통구 (6개)

| 동 | 코드 |
|---|---|
| 광교1동 | `광교1동-1302` |
| 망포동 | `망포동-4534` |
| 매탄동 | `매탄동-4535` |
| 영통동 | `영통동-4537` |
| 원천동 | `원천동-1298` |
| 이의동 | `이의동-4538` |

### 수원시 팔달구 (6개)

| 동 | 코드 |
|---|---|
| 고등동 | `고등동-1285` |
| 매교동 | `매교동-1283` |
| 우만동 | `우만동-4558` |
| 인계동 | `인계동-1291` |
| 화서2동 | `화서2동-1287` |
| 화서동 | `화서동-4564` |

### 시흥시 (2개)

| 동 | 코드 |
|---|---|
| 배곧동 | `배곧동-4245` |
| 정왕동 | `정왕동-4578` |

### 안산시 단원구 (2개)

| 동 | 코드 |
|---|---|
| 고잔동 | `고잔동-1517` |
| 초지동 | `초지동-1523` |

### 안산시 상록구 (1개)

| 동 | 코드 |
|---|---|
| 사동 | `사동-1504` |

### 안양시 동안구 (1개)

| 동 | 코드 |
|---|---|
| 호계동 | `호계동-4640` |

### 용인시 기흥구 (4개)

| 동 | 코드 |
|---|---|
| 구갈동 | `구갈동-1679` |
| 동백동 | `동백동-1685` |
| 보정동 | `보정동-1687` |
| 신갈동 | `신갈동-1677` |

### 용인시 수지구 (2개)

| 동 | 코드 |
|---|---|
| 죽전동 | `죽전동-4719` |
| 풍덕천동 | `풍덕천동-4720` |

### 의정부시 (1개)

| 동 | 코드 |
|---|---|
| 의정부동 | `의정부동-4745` |

### 파주시 (1개)

| 동 | 코드 |
|---|---|
| 야당동 | `야당동-4774` |

### 평택시 (2개)

| 동 | 코드 |
|---|---|
| 동삭동 | `동삭동-4784` |
| 비전동 | `비전동-4786` |

### 화성시 동탄구 (3개)

| 동 | 코드 |
|---|---|
| 동탄1동 | `동탄1동-1786` |
| 여울동 | `오산동-4844` |
| 청계동 | `청계동-4847` |

### 화성시 만세구 (1개)

| 동 | 코드 |
|---|---|
| 향남읍 | `향남읍-1768` |

---

# 2. 번개장터 — 검색 API 동작 & 서울·경기 전량 크롤

실험 일자: 2026-07-17. Oracle 서버(`howmuch-backend` 컨테이너)와 로컬에서 실제 요청으로 검증했다.

## 검색 API 기본

```
GET https://api.bunjang.co.kr/api/1/find_v2.json
헤더: User-Agent: Mozilla/5.0, Referer: https://m.bunjang.co.kr/
```

| 파라미터 | 설명 |
|---|---|
| `q` | 검색어 |
| `order` | 정렬 (아래 표) |
| `page` / `n` | 페이지 번호 / 페이지 크기 |
| 응답 | `num_found`(총 건수), `list`(매물 배열) |

## 핵심 발견 ①: 기존 코드의 `lat`/`lon`/`distance`는 아무 효과가 없다

기존 `bunjang.py`는 `lat=37.5665, lon=126.9780, distance=0`을 항상 붙였지만,
**좌표를 천호동/수원으로 바꾸고 distance를 0/3/3000으로 바꿔도 결과가 완전히 동일**했다
(num_found·매물 목록 모두 같음). 즉 위치 필터링은 처음부터 동작한 적이 없고 실제로는 전국 검색이었다.

## 핵심 발견 ②: `order=distance`는 좌표 기준 "거리순 정렬"이다 (필터 아님)

`order=distance` + `lat`/`lon`을 주면 num_found는 그대로지만 **가까운 매물부터 정렬**된다.
서울시청(37.5665, 126.9780) 기준 "아이폰 14 프로" 검색(n=100) 실측:

| 페이지 | 내용 |
|---|---|
| 0 | 서울 100건 (도심부터) |
| 3 | 서울 94 + 경기 6 (경계 진입) |
| 10 | 전부 지방 (충북·대전) |
| 20~ | 전부 위치 없음 (맨 뒤로 밀림) |
| 60 | 빈 페이지 (num_found=4006 소진) |

→ **한 중심점에서 페이지를 넘기다 서울/경기 매물이 안 나오면 중단**하는 것만으로
지역 내 위치 등록 매물을 전량 수집할 수 있다. 동(행정구역)별 반복 검색이 필요 없다.

## 정렬(`order`) 유효값

| 값 | 동작 |
|---|---|
| `date` | 최신순 (기본) |
| `score` | 정확도순 — num_found가 더 크게 나옴 (매칭이 느슨해짐: 4006 → 4460) |
| `distance` | `lat`/`lon` 기준 거리순. 위치 없는 매물은 맨 뒤 |
| `price_asc` / `price_desc` | 가격 오름/내림차순 |
| `popular` | 인기순 |

## 페이지네이션 한계

- `n`(페이지 크기) 최대 **200** (`n=300`은 HTTP 400)
- 접근 가능한 오프셋(page×n)은 **10,000 미만** (offset 9,900은 OK, 10,000은 400)
  - 즉 한 검색어로 최대 10,000건까지만 볼 수 있다. "아이폰"처럼 num_found가 61,177인
    검색어는 전체 열람이 불가능하므로, 필터나 정렬로 잘라서 접근해야 한다.
  - 모델 단위 검색어(num_found 수천 건)는 한계 안에 넉넉히 들어온다.

## 동작하는 필터 파라미터 (실측)

"아이폰 14 프로" 기준 num_found 4,006에서의 변화:

| 파라미터 | 효과 | 실측 |
|---|---|---|
| `f_price_min` | 최소 가격 | 500000 → 1,560건 |
| `f_price_max` | 최대 가격 | 500000 → 2,522건 |
| `f_category_id` | 카테고리 한정 | 600700001 → 1,864건 |
| `f_used=2` | 새상품(미개봉류)만 | 1,054건 |

무효(무시됨): `price_min/max`, `category_id`, `used`, `f_bizseller`, `free_shipping`,
그리고 위의 `lat`/`lon`/`distance`.

## 매물 필드 관찰

- `location`: 검색 결과(최신순)에서 **약 25%만 보유**. 직거래 위치를 등록한 매물만 있다.
- `status`: 검색 결과는 전부 `"0"` — 판매중만 노출되는 것으로 보임.
- `only_neighborhood`: 표본 100건에서 0건.
- `ad`: 이번 크롤 결과 7,366건 중 광고 0건 (거리순 정렬에는 광고가 섞이지 않는 듯).
- 상세 API `GET /api/pms/v1/products/{pid}/detail/web`의 `data.product.geo`에
  **정확한 좌표(lat/lon)와 행정동 주소**가 있다 (예: `서울특별시 종로구 종로5.6가동`).

## 키워드 토큰화

공백 변형은 결과가 거의 동일하다: `아이폰 14 프로` 4,006 / `아이폰14프로` 4,143 /
`iPhone 14 Pro` 4,006 / `iphone14pro` 3,993. 별칭 키워드의 추가 효과는 크지 않다
(크롤에서는 pid로 전역 중복 제거).

## 서울·경기 전량 크롤 방법론 & 결과 (2026-07-17)

- 실행: Oracle 서버 `howmuch-backend` 컨테이너, `PYTHONPATH=/app`으로
  앱의 `CRAWL_TARGETS`(72개 타겟, 키워드 227개)와 `matches_target_listing` 필터 재사용
- 방식: 타겟×키워드마다 서울시청 중심 `order=distance`, `n=100`으로 페이지 순회.
  서울/경기 매물 0건 페이지가 2연속이면 해당 키워드 중단. pid 전역 중복 제거.
- 요청 총 1,188건, 간격 0.35s, **429 차단 없음**

결과: **7,366건** (서울 3,798 / 경기 3,568)

| 카테고리 | 건수 |
|---|---|
| iPhone | 3,047 |
| iPad | 1,316 |
| AirPods | 1,162 |
| AppleWatch | 1,032 |
| MacBook | 809 |

모델 상위: iPhone 15 Pro 453, iPhone 14 Pro 451, AirPods Pro 2세대 434,
iPhone 16 Pro 301, Apple Watch SE 2세대 298.
시군구 상위: 성북구 633, 수원시 357, 고양시 305, 부천시 254, 강남구 252.

CSV: 서버 `~/HowMuchApple/exports/bunjang_seoul_gyeonggi_crawl.csv`
(로컬 사본 `backend/exports/`). 기존 크롤러의 모델당 30건 상한과 비교하면
동일 지역 기준 수집량이 수십 배다.

## 검증: 동네별 검색 vs 단일 중심점 검색은 결과가 동일하다

"아이폰 14 프로"를 서로 다른 중심점 3곳(서울시청 / 수원 인계동 / 의정부 의정부동)에서
각각 거리순 전체 순회(41페이지)한 뒤, 수집된 서울/경기 매물 pid 집합을 비교했다:

- 세 중심점 모두 **정확히 같은 1,001건** (집합 차이 0건)
- 기준 변동폭(같은 중심점 2회 순회 차이)도 0건 — 시간차 노이즈 아님

`order=distance`는 동일한 전국 결과 집합을 정렬만 바꿔 보여주는 것이므로,
어느 좌표를 중심으로 잡아도 서울/경기 매물의 집합은 불변이다.
동네별 반복 검색은 추가 매물 없이 요청 수만 동네 수만큼 배가된다.

## 기존 `bunjang.py` 개선 포인트

1. 무효 파라미터 `lat`/`lon`/`distance` 제거, 지역 수집이 목적이면 `order=distance` + 중단 조건으로 전환
2. `n=100` → `n=200`으로 요청 수 절반 감축 가능
3. 별칭 키워드는 수집 증분이 작으므로 우선순위 낮음 (primary만으로 대부분 커버)

---

# 3. 중고나라 — 전국 전량 수집 + 지역 라벨 필터

> 2026-07-17 작성. 서울·경기 매물 최대 수집을 위해 중고나라의 지역 검색 지원 여부를 조사하고,
> 전국 전량 수집 + 지역 라벨 필터 방식을 설계·실행한 기록.

## 한 줄 요약

중고나라는 당근처럼 "지역을 지정해서" 검색할 수 없다. 대신 검색 API가 **전체 결과를 페이지네이션으로 끝까지 내어주고, 매물마다 판매자 지역(`locationNames`)이 붙어 나오므로**, 전국을 통째로 수집한 뒤 지역 라벨로 서울/경기를 골라내는 방식을 쓴다.

## 왜 이 방식인가 (조사 결과)

당근은 `?in=<동이름-코드>`로 동네를 지정해야만 그 지역 매물이 나오는 구조라 동 코드 185개를 순회한다 (→ 1장). 중고나라는 구조가 다르다:

| 시도 | 결과 |
|---|---|
| URL 지역 파라미터 | 없음. 웹 프론트(데스크톱/모바일)에 지역 필터 UI 자체가 없음 (앱 전용 기능) |
| 검색 API의 `locationFilter` 필드 | 필드는 존재하나(400 타입에러로 확인) 스키마가 앱 전용. 객체/문자열/좌표/행정코드 등 10여 가지 형식을 넣어봤지만 전부 500 → 사용 불가 |
| 검색어에 동 이름 포함 (`"천호동 아이폰 14 프로"`) | ✅ 동작함. 제목에 동 이름이 없어도 매물의 구조화된 지역 필드와 매칭됨 (천호제1동·천호제3동 매물만 반환) |
| 전국 검색 + `locationNames` 후처리 | ✅ **채택.** 동 키워드 검색의 상위집합임을 검증 (천호동 3건이 전국 1,151건에 전부 포함). 요청 수는 동별 순회의 약 1/7 |

동별 검색은 결국 전국 검색의 부분집합이므로, 전국 전량 수집이 누락 없이 더 적은 요청으로 같은 목적을 달성한다.

## 검색 API 스펙

```
POST https://search-api.joongna.com/v3/search/all
Content-Type: application/json
Origin: https://web.joongna.com
```

요청 body (웹 프론트가 실제로 보내는 형식을 그대로 사용):

```json
{
  "osType": 2,
  "firstQuantity": 50, "quantity": 50,
  "jnPayYn": "ALL",
  "categoryFilter": [{"categoryDepth": 0, "categorySeq": 0}],
  "priceFilter": {"minPrice": 0, "maxPrice": 100000000},
  "sort": "RECENT_SORT",
  "saleYn": "SALE_N", "parcelFeeYn": "ALL",
  "page": 0,
  "searchWord": "아이폰 14 프로",
  "adjustSearchKeyword": true,
  "keywordSource": "INPUT_KEYWORD",
  "registPeriod": "ALL"
}
```

응답의 `data.items[]`에서 사용하는 필드:

| 필드 | 용도 |
|---|---|
| `seq` | 상품 고유번호. 중복 제거 키, 상품 URL(`https://web.joongna.com/product/{seq}`) |
| `title`, `price` | 제목/가격. 별도 파싱 불필요 |
| `locationNames` | 판매자 지역, 예: `["서울특별시 강동구 천호제3동"]`. 택배 전용 판매자는 빈 배열 |
| `objectType` | `"product"`만 수집 (광고·게시글 제외) |

## 수집 파이프라인

1. **검색 단위 = 모델 키워드.** 타겟 72개 모델의 별칭 포함 총 227개 키워드 (예: iPhone SE 3세대 → `아이폰 SE 3세대`, `아이폰SE3`, `iPhone SE 3`, `iPhone SE 2022`). 동시에 4개 키워드씩 병렬 진행.
2. **키워드당 페이지 전량 순회.** `page`를 0부터 올리며 50개씩 요청. 신규 매물이 2페이지 연속 안 나오면 종료 (안전장치: 최대 40페이지). 페이지 간 0.3초 대기, 429/5xx 응답 시 10초+ 백오프 후 재시도 (최대 3회).
3. **필터링.** `objectType == "product"`, 가격 > 0, 그리고 기존 `matches_target_listing()`으로 제목·가격이 타겟 모델과 실제로 맞는지 검증 — "아이폰 14 프로 케이스" 같은 액세서리 제거.
4. **중복 제거.** `seq` 기준 전역 dedup. 같은 매물이 여러 키워드에 걸려도 1건 (먼저 매칭된 타겟 소속).
5. **지역 라벨링.** `locationNames[0]`을 그대로 저장, 시도(서울/경기/...)별 분류는 후처리. 상세페이지 조회 불필요 — 기존 크롤러(`app/crawlers/joongna.py`)가 매물마다 상세페이지를 열어 지역을 뽑던 과정이 통째로 사라진다.

### 페이지네이션 전량 수집 검증

"아이폰 14 프로" 단일 키워드로 사전 검증: 전체 1,151건 중 1,124건(97.7%)이 25페이지 만에 수집됨. 누락분은 크롤 도중 올라온 신규 매물과 정렬 흔들림 수준. 즉 이 API는 검색 결과를 잘라내지 않는다.

## 실행 결과 (2026-07-17)

- 총 요청 약 2,500회 (키워드당 평균 ~11페이지), 소요 약 12분, 차단 없음
- **고유 매물 6,288건** → `backend/exports/joongna_all_regions_crawl.csv`
  (컬럼: `external_id, category, model, title, price, region, search_keyword, url`)

| 지역 | 매물 수 |
|---|---|
| 서울특별시 | 1,392 |
| 경기도 | 1,308 |
| **서울/경기 소계** | **2,700** |
| 지역 미표기 (택배거래 판매자) | 1,693 |
| 그 외 지방 | 1,895 |

모델 상위: AirPods Pro 2 (605), AirPods Pro 3 (509), iPhone 15 (331), Watch SE 2 (318), iPhone 14 (285), …

기존 Playwright 방식(모델당 30개 상한, 스크롤 + 상세페이지 조회) 대비: 상한 없이 전량 수집, 브라우저 불필요, 매물당 상세 요청 제거.

## 크롤러 본체 교체 (2026-07-17)

위 방식을 검증한 뒤 `app/crawlers/joongna.py`를 전면 재작성했다. 클래스명(`JoognaCrawler`)과
반환 형식(`CrawledItem`)은 그대로라 `run.py`/`base.py` 등 호출부 변경은 없다.

| | 기존 (Playwright 방식) | 교체 후 (API 방식) |
|---|---|---|
| 수집 경로 | 브라우저로 검색 페이지 열어 무한스크롤 + HTML 정규식 파싱 (+ httpx HTML 폴백) | `search-api` POST 호출, JSON 응답 파싱 |
| 지역 정보 | 매물마다 **상세페이지를 별도 요청**해 `locationName`/`dongCode` 추출 | 검색 응답의 `locationNames`를 바로 사용 (매물당 추가 요청 0회) |
| 수집량 | 타겟(모델)당 30개 상한 (`default_items_per_target`) | 상한 없음 — 키워드별 전 페이지 순회, `max_items` 지정 시에만 제한 |
| 키워드 | 타겟당 키워드 순회하되 30개 차면 중단 | 별칭 포함 전 키워드 순회 + `seq` 기준 중복 제거 |
| 의존성 | playwright(chromium) + httpx | httpx만 |
| dong_code | 상세페이지에서 채움 | **채우지 않음** (`None`) — DB의 지역 매칭(`emd_id`)은 `region_name` 텍스트 파싱으로 동작하므로 영향 없음. 행정동 코드가 꼭 필요하면 상세페이지 조회를 별도 후처리로 |
| 정확도 필터 | `matches_target_listing()` | 동일하게 유지 + `objectType == "product"`(광고·게시글 제외) 추가 |
| 요청 제어 | 스크롤 대기 위주 | 동시 4타겟, 페이지 간 0.3초, 429/5xx 백오프 재시도 |

검증: 타겟 2개(iPhone 14 Pro, iPhone 15) 제한 실행으로 매물 수집·지역 라벨·중복 제거·`max_items` 상한 동작 확인.

## 참고: 매물 상세의 지역 정보

상세 페이지(`web.joongna.com/product/{seq}`) HTML에는 `locationName`, `dongCode`(행정동 코드)가 들어 있으며 기존 `app/crawlers/joongna.py`의 `_fetch_detail_regions()`가 이를 사용한다. 검색 API가 `locationNames`를 바로 주므로 대량 수집에는 불필요하지만, 행정동 코드가 필요한 경우엔 이 경로를 쓴다.

---

# 관련 자료

- 당근 동별 수집 스크립트: `backend/scripts/crawl_daangn_all_dongs.py`
  (원본 문서 `docs/daangn_seoul_gyeonggi_region_codes.md`를 읽어 동작)
- 중고나라 전량 수집 스크립트: `backend/scripts/crawl_joongna_all.py` (수동 실행/DB 미사용)
- 크롤 결과 CSV: `backend/exports/bunjang_seoul_gyeonggi_crawl.csv`, `backend/exports/joongna_all_regions_crawl.csv`
- 원본 문서: `docs/daangn_seoul_gyeonggi_region_codes.md`, `docs/bunjang_api_behavior.md`, `docs/joongna_crawling_method.md`
